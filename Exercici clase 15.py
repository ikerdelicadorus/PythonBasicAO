#Donada una llista que ens retorni que sigui parells
"""
l=[3, 5, 6, 8, 9, 11, 12]

r=[]
for e in l:
    if e%2==0:
        r.append(e)
print(r)

#Versió en filter(programació funcional)
def parell(x):
    if x%2==0
        return True
    return False
r=list(filter(parel,l))
print(r)

#Versió amb funció anònima
R=list(filter(lambda x:x%2==0,l))
print(s)
"""